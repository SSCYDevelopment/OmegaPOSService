# CRM01 SQL 脚本

此目录用于存放与 OMEGAPOS 模块相关的 SQL 脚本和存储过程。

说明：
- 文件命名请保持清晰、有版本或用途标识（例如 `MPos_Crm01_GetSuspend.sql`）。
- 在将脚本部署到生产库前，请先在测试库验证并记录变更历史。

已包含（如果需要，请补充或删除）：

使用与部署：
1. 将 .sql 文件在测试库执行并确认无误。
2. 将变更记录添加到版本控制（Commit）并在合并请求中说明影响范围。
3. 在生产部署时遵循运维流程并备份目标数据库。

联系人：
- 若需帮助，请联系本项目维护者或在仓库中打开 Issue。